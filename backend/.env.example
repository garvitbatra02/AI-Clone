# Multi-API Key Rotation System - Environment Configuration

# ============================================================
# API KEYS CONFIGURATION (Comma-separated)
# ============================================================

# Groq API Keys (comma-separated for rotation)
# Get your keys from: https://console.groq.com/keys
GROQ_API_KEYS="your_groq_key_1,your_groq_key_2,your_groq_key_3"

# Cerebras API Keys (comma-separated for rotation)
# Get your keys from: https://cloud.cerebras.ai/
CEREBRAS_API_KEYS="your_cerebras_key_1,your_cerebras_key_2,your_cerebras_key_3"

# ============================================================
# SYSTEM BEHAVIOR
# ============================================================

# Path A (Default Models):
# - No model parameter in request
# - Uses default models: llama-3.3-70b-versatile (Groq), llama-3.3-70b (Cerebras)
# - Provider-level rotation: Groq -> Cerebras -> Groq...
# - Cached LLM instances per (provider, key_index)
# - Fallback: tries all providers before failing

# Path B (Specific Models):
# - Model parameter specified in request
# - Uses the exact model requested
# - Key-level rotation: tries all API keys for that provider
# - On-demand LLM instance creation (not cached)
# - Fallback: tries all keys for that model before failing
# - Does NOT switch providers or models

# ============================================================
# EXAMPLE CONFIGURATIONS
# ============================================================

# Single key per provider (basic setup):
# GROQ_API_KEYS="gsk_abc123"
# CEREBRAS_API_KEYS="csk_xyz789"

# Multiple keys per provider (recommended for production):
# GROQ_API_KEYS="gsk_key1,gsk_key2,gsk_key3"
# CEREBRAS_API_KEYS="csk_key1,csk_key2,csk_key3"

# Mixed configuration (different number of keys):
# GROQ_API_KEYS="gsk_key1,gsk_key2"
# CEREBRAS_API_KEYS="csk_key1,csk_key2,csk_key3,csk_key4"
